BACKEND NEXT STEP

Frontend is intentionally ready first so you can test the visual experience immediately.

Recommended production path:
GitHub Pages -> Cloudflare Worker -> Google Sheet

The frontend already sends JSON events to `apiEndpoint` if configured.
